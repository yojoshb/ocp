# Optional - Override the chart name if desired
fullnameOverride: ""
nameOverride: ""

general:
  domain: "lab.io" # OCP: Set as Domain name for the cluster
  ingress:
    enabled: false # OCP: Set to false to utilize Routes
    # Current supported values for ingress type include: nginx
    className: "nginx"
     ## - Annotations to add to the Ingress resource.
    annotations: {}
      # nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
      # nginx.ingress.kubernetes.io/use-regex: "true"
      # nginx.ingress.kubernetes.io/rewrite-target: /$1
      # nginx.ingress.kubernetes.io/proxy-body-size: 500m
      # nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
      # nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
      # nginx.ingress.kubernetes.io/proxy-connect-timeout: "600"
    ## - Labels to add to the Ingress resource
    labels: {}
    # Certificate options
    tls:
      # TLS certificate secret name
      name: ""
      # Cluster cert issuer (ex. Let's Encrypt) name if one exists
      clusterIssuer: ""
    # Ingress path configuration - The samples provided are for Nginx. Examples with other Ingress providers are in the chart Readme on GitHub
    paths:
      web:
        path: /(.*)
        pathType: ImplementationSpecific
      attachments:
        path: /attachments/(.*)
        pathType: ImplementationSpecific
      api:
        path: /api/(.*)
        pathType: ImplementationSpecific
      icons:
        path: /icons/(.*)
        pathType: ImplementationSpecific
      notifications:
        path: /notifications/(.*)
        pathType: ImplementationSpecific
      events:
        path: /events/(.*)
        pathType: ImplementationSpecific
      scim:
        path: /scim/(.*)
        pathType: ImplementationSpecific
      sso:
        path: /(sso/.*)
        pathType: ImplementationSpecific
      identity:
        path: /(identity/.*)
        pathType: ImplementationSpecific
      admin:
        path: /(admin/?.*)
        pathType: ImplementationSpecific

  # Override application container version tags.  Theses will be versioned with the chart and need not be changed in most circumstances.
  coreVersionOverride: ""
  webVersionOverride: ""
  # Disable account registration on web vault
  disableUserRegistration: "false"
  # Comma-separated list of email addresses for Admin users
  admins: "false"
  email:
    # Email address used for invitations, typically no-reply@smtp-host
    replyToEmail: "no-reply@bitwarden.localhost"
    # Your SMTP server hostname (recommended) or IP address
    smtpHost: "smtp.gmail.com"
    # The SMTP port used by the SMTP server
    smtpPort: "587"
    # Whether your SMTP server uses an encryption protocol, "true" for SSL, "false" for TLS
    smtpSsl: "false"
    # Specify true to explicitly trust the certificate presented by the SMTP server (not recommended for production).
    smtpTrustServer: "false"
    # Specify true to use SSL (not TLS) on port 25.
    smtpSslOverride: "false"
    # Specify true to force STARTTLS (Opportunistic TLS).
    smtpStartTls: "false"
  sso:
    enforceSsoPolicyForAllUsers: false
  # Custom labels to add throughout the installation
  labels: {}
  # Custom annotations to add throughout the installation
  annotations: {}
  # Additional default pod labels
  podLabels: {}
  # Additional default pod annotations
  podAnnotations: {}
  # nodeSelector and tolerations defined here will apply to all templates in the Helm chart. They can be individually overridden
  nodeSelector: {}
  tolerations: []
  # Specifies the access mode for persistent volume claims.  This should not be changed in most cases, and the allowable
  # values are only ReadWriteMany and ReadWriteOnce.  Please read https://kubernetes.io/docs/concepts/storage/persistent-volumes/#access-modes
  # to better understand these options before changing this value.
  volumeAccessMode: "ReadWriteMany"
  # Set to true to allow communication between your server and our cloud system. Doing so enables billing and license sync.  Please see: https://bitwarden.com/help/self-host-an-organization/#step-4-setup-billing-and-license-sync
  enableCloudCommunication: false
  # Cloud region for sync.  Please see: https://bitwarden.com/help/families-for-enterprise-self-hosted/#step-1-enable-cloud-communication
  cloudRegion: US

  # Refresh token variables https://bitwarden.com/help/environment-variables/#refresh-token-variables
  refreshTokens:
    # Set to true to enable refresh tokens.  Recommended for production environments.
    enabled: false
    applyAbsoluteRefreshTokenOnRefreshToken: "true"
    absoluteRefreshTokenLifetimeSeconds: "30"
    slidingRefreshTokenLifetimeSeconds: ""

# Specify the name of the shared storage class
# This storage class requires ReadWriteMany.  You will need to provide your own storage class.  Storage classes with automatic volume previsioners are recommended.
  sharedStorageClassName: "ocs-storagecluster-cephfs" # OCP: RWX required for this, use whatever persistant storage you have that can do RWX

# Secrets are required.  Review the chart README on GitHub for details on creating these secrets
secrets:
  secretName: custom-secret
  # Secrets can either be created via 'kubectl create secrets' or by using a secret provider class.  If using the latter, specify the name here
  secretProviderClass:

# Settings for individual service containers
component:
  # The Admin component
  admin:
    # Additional deployment labels
    labels: {}
    # Additional deployment annotations
    annotations: {}
    # Additional pod labels
    podLabels: {}
    # Additional pod annotations
    podAnnotations: {}
    # Optionally place deployment on specifc nodes
    nodeSelector: {}
    # Optionally add taint tolerations
    tolerations: []
    # Image name, tag, and pull policy
    image:
      name: registry.lab.io:8443/bitwarden/admin # mirrored-image
    # The pod resource requirements.  You can adjust these up and down for your environment
    resources:
      requests:
        memory: "64Mi"
        cpu: "50m"
      limits:
        memory: "128Mi"
        cpu: "100m"
    # Specifies the strategy used to replace old Pods by new ones. The value can be "Recreate" or "RollingUpdate". "RollingUpdate" is the default value.
    deploymentStrategy: RollingUpdate
    # This will set the Kubernetes container security context
    securityContext: {}
    # This will set the Kubernetes pod security context
    podSecurityContext: {}
    # Run the pod under a service account you create.  This is especially useful for OpenShift deployments
    podServiceAccount: ""
  api:
    # Additional deployment labels
    labels: {}
    # Additional deployment annotations
    annotations: {}
    # Additional pod labels
    podLabels: {}
    # Additional pod annotations
    podAnnotations: {}
    # Optionally place deployment on specifc nodes
    nodeSelector: {}
    # Optionally add taint tolerations
    tolerations: []
    # Image name, tag, and pull policy
    image:
      name: registry.lab.io:8443/bitwarden/api # mirrored-image
    # The pod resource requirements.  You can adjust these up and down for your environment
    resources:
      requests:
        memory: "128Mi"
        cpu: "100m"
      limits:
        memory: "256Mi"
        cpu: "200m"
    # Specifies the strategy used to replace old Pods by new ones. The value can be "Recreate" or "RollingUpdate". "RollingUpdate" is the default value.
    deploymentStrategy: RollingUpdate
    # This will set the Kubernetes container security context
    securityContext: {}
    # This will set the Kubernetes pod security context
    podSecurityContext: {}
    # Run the pod under a service account you create.  This is especially useful for OpenShift deployments
    podServiceAccount: ""
  attachments:
    # Additional deployment labels
    labels: {}
    # Additional deployment annotations
    annotations: {}
    # Additional pod labels
    podLabels: {}
    # Additional pod annotations
    podAnnotations: {}
    # Optionally place deployment on specifc nodes
    nodeSelector: {}
    # Optionally add taint tolerations
    tolerations: []
    # Image name, tag, and pull policy
    image:
      name: registry.lab.io:8443/bitwarden/attachments # mirrored-image
    # The pod resource requirements.  You can adjust these up and down for your environment
    resources:
      requests:
        memory: "64Mi"
        cpu: "50m"
      limits:
        memory: "128Mi"
        cpu: "100m"
    # Specifies the strategy used to replace old Pods by new ones. The value can be "Recreate" or "RollingUpdate". "RollingUpdate" is the default value.
    deploymentStrategy: RollingUpdate
    # This will set the Kubernetes container security context
    securityContext: {}
    # This will set the Kubernetes pod security context
    podSecurityContext: {}
    # Run the pod under a service account you create.  This is especially useful for OpenShift deployments
    podServiceAccount: ""
  events:
    # Additional deployment labels
    labels: {}
    # Additional deployment annotations
    annotations: {}
    # Additional pod labels
    podLabels: {}
    # Additional pod annotations
    podAnnotations: {}
    # Optionally place deployment on specifc nodes
    nodeSelector: {}
    # Optionally add taint tolerations
    tolerations: []
    # Image name, tag, and pull policy
    image:
      name: registry.lab.io:8443/bitwarden/events # mirrored-image
    # The pod resource requirements.  You can adjust these up and down for your environment
    resources:
      requests:
        memory: "64Mi"
        cpu: "50m"
      limits:
        memory: "128Mi"
        cpu: "100m"
    # Specifies the strategy used to replace old Pods by new ones. The value can be "Recreate" or "RollingUpdate". "RollingUpdate" is the default value.
    deploymentStrategy: RollingUpdate
    # This will set the Kubernetes container security context
    securityContext: {}
    # This will set the Kubernetes pod security context
    podSecurityContext: {}
    # Run the pod under a service account you create.  This is especially useful for OpenShift deployments
    podServiceAccount: ""
  icons:
    # Additional deployment labels
    labels: {}
    # Additional deployment annotations
    annotations: {}
    # Additional pod labels
    podLabels: {}
    # Additional pod annotations
    podAnnotations: {}
    # Optionally place deployment on specifc nodes
    nodeSelector: {}
    # Optionally add taint tolerations
    tolerations: []
    # Image name, tag, and pull policy
    image:
      name: registry.lab.io:8443/bitwarden/icons # mirrored-image
    # The pod resource requirements.  You can adjust these up and down for your environment
    resources:
      requests:
        memory: "64Mi"
        cpu: "50m"
      limits:
        memory: "128Mi"
        cpu: "100m"
    # Specifies the strategy used to replace old Pods by new ones. The value can be "Recreate" or "RollingUpdate". "RollingUpdate" is the default value.
    deploymentStrategy: RollingUpdate
    # This will set the Kubernetes container security context
    securityContext: {}
    # This will set the Kubernetes pod security context
    podSecurityContext: {}
    # Run the pod under a service account you create.  This is especially useful for OpenShift deployments
    podServiceAccount: ""
  identity:
    # Additional deployment labels
    labels: {}
    # Additional deployment annotations
    annotations: {}
    # Additional pod labels
    podLabels: {}
    # Additional pod annotations
    podAnnotations: {}
    # Optionally place deployment on specifc nodes
    nodeSelector: {}
    # Optionally add taint tolerations
    tolerations: []
    # Image name, tag, and pull policy
    image:
      name: registry.lab.io:8443/bitwarden/identity # mirrored-image
    # The pod resource requirements.  You can adjust these up and down for your environment
    resources:
      requests:
        memory: "128Mi"
        cpu: "100m"
      limits:
        memory: "256Mi"
        cpu: "200m"
    # Specifies the strategy used to replace old Pods by new ones. The value can be "Recreate" or "RollingUpdate". "RollingUpdate" is the default value.
    deploymentStrategy: RollingUpdate
    # This will set the Kubernetes container security context
    securityContext: {}
    # This will set the Kubernetes pod security context
    podSecurityContext: {}
    # Run the pod under a service account you create.  This is especially useful for OpenShift deployments
    podServiceAccount: ""
  notifications:
    # Additional deployment labels
    labels: {}
    # Additional deployment annotations
    annotations: {}
    # Additional pod labels
    podLabels: {}
    # Additional pod annotations
    podAnnotations: {}
    # Optionally place deployment on specifc nodes
    nodeSelector: {}
    # Optionally add taint tolerations
    tolerations: []
    # Image name, tag, and pull policy
    image:
      name: registry.lab.io:8443/bitwarden/notifications # mirrored-image
    # The pod resource requirements.  You can adjust these up and down for your environment
    resources:
      requests:
        memory: "64Mi"
        cpu: "50m"
      limits:
        memory: "128Mi"
        cpu: "100m"
    # Specifies the strategy used to replace old Pods by new ones. The value can be "Recreate" or "RollingUpdate". "RollingUpdate" is the default value.
    deploymentStrategy: RollingUpdate
    # This will set the Kubernetes container security context
    securityContext: {}
    # This will set the Kubernetes pod security context
    podSecurityContext: {}
    # Run the pod under a service account you create.  This is especially useful for OpenShift deployments
    podServiceAccount: ""
  scim:
    # SCIM is disabled by default.  To use this service, enable it below and set an appropriate Ingress path
    enabled: false
    # Additional deployment labels
    labels: {}
    # Additional deployment annotations
    annotations: {}
    # Additional pod labels
    podLabels: {}
    # Additional pod annotations
    podAnnotations: {}
    # Optionally place deployment on specifc nodes
    nodeSelector: {}
    # Optionally add taint tolerations
    tolerations: []
    # Image name, tag, and pull policy
    image:
      name: registry.lab.io:8443/bitwarden/scim # mirrored-image
    # The pod resource requirements.  You can adjust these up and down for your environment
    resources:
      requests:
        memory: "64Mi"
        cpu: "50m"
      limits:
        memory: "128Mi"
        cpu: "100m"
    # Specifies the strategy used to replace old Pods by new ones. The value can be "Recreate" or "RollingUpdate". "RollingUpdate" is the default value.
    deploymentStrategy: RollingUpdate
    # This will set the Kubernetes container security context
    securityContext: {}
    # This will set the Kubernetes pod security context
    podSecurityContext: {}
    # Run the pod under a service account you create.  This is especially useful for OpenShift deployments
    podServiceAccount: ""
  sso:
    # Additional deployment labels
    labels: {}
    # Additional deployment annotations
    annotations: {}
    # Additional pod labels
    podLabels: {}
    # Additional pod annotations
    podAnnotations: {}
    # Optionally place deployment on specifc nodes
    nodeSelector: {}
    # Optionally add taint tolerations
    tolerations: []
    # Image name, tag, and pull policy
    image:
      name: registry.lab.io:8443/bitwarden/sso # mirrored-image
    # The pod resource requirements.  You can adjust these up and down for your environment
    resources:
      requests:
        memory: "64Mi"
        cpu: "50m"
      limits:
        memory: "128Mi"
        cpu: "100m"
    # Specifies the strategy used to replace old Pods by new ones. The value can be "Recreate" or "RollingUpdate". "RollingUpdate" is the default value.
    deploymentStrategy: RollingUpdate
    # This will set the Kubernetes container security context
    securityContext: {}
    # This will set the Kubernetes pod security context
    podSecurityContext: {}
    # Run the pod under a service account you create.  This is especially useful for OpenShift deployments
    podServiceAccount: ""
  web:
    # Additional deployment labels
    labels: {}
    # Additional deployment annotations
    annotations: {}
    # Additional pod labels
    podLabels: {}
    # Additional pod annotations
    podAnnotations: {}
    # Optionally place deployment on specifc nodes
    nodeSelector: {}
    # Optionally add taint tolerations
    tolerations: []
    # Image name, tag, and pull policy
    image:
      name: registry.lab.io:8443/bitwarden/web # mirrored-image
    # The pod resource requirements.  You can adjust these up and down for your environment
    resources:
      requests:
        memory: "64Mi"
        cpu: "50m"
      limits:
        memory: "128Mi"
        cpu: "100m"
    # Specifies the strategy used to replace old Pods by new ones. The value can be "Recreate" or "RollingUpdate". "RollingUpdate" is the default value.
    deploymentStrategy: RollingUpdate
    # This will set the Kubernetes container security context
    securityContext: {}
    # This will set the Kubernetes pod security context
    podSecurityContext: {}
    # Run the pod under a service account you create.  This is especially useful for OpenShift deployments
    podServiceAccount: ""
# Images used for jobs and init containers
supportComponents:
  dbMigrator:
    image:
      name: registry.lab.io:8443/bitwarden/mssqlmigratorutility # mirrored-image
  certGenerator:
    image:
      name: registry.lab.io:8443/alpine/openssl # mirrored-image
      tag: 3.5.2
  kubectl:
    image:
      name: registry.lab.io:8443/bitwarden/helm-charts/kubectl # mirrored-image
      tag: 1.34

# Data volume sizes for shared PVCs
volume:
  dataprotection:
    # Use an existing PVC by specifying the name.
    # existingClaim: claimName
    # Override the accessMode specified in general.volumeAccessMode
    # accessMode: ReadWriteOnce
    # Override the storageClass specified in sharedStorageClassName
    # storageClass: "shared-storage"
    size: "1Gi"
    labels: {}
  attachments:
    # Use an existing PVC by specifying the name.
    # existingClaim: claimName
    # Override the accessMode specified in general.volumeAccessMode
    # accessMode: ReadWriteOnce
    # Override the storageClass specified in sharedStorageClassName
    # storageClass: "shared-storage"
    size: 1Gi
    labels: {}
  licenses:
    # Use an existing PVC by specifying the name.
    # existingClaim: claimName
    # Override the accessMode specified in general.volumeAccessMode
    # accessMode: ReadWriteOnce
    # Override the storageClass specified in sharedStorageClassName
    # storageClass: "shared-storage"
    size: 1Gi
    labels: {}
  logs:
    # Logs are disabled by default
    enabled: false
    # Use an existing PVC by specifying the name.
    # existingClaim: claimName
    # Override the accessMode specified in general.volumeAccessMode
    # accessMode: ReadWriteOnce
    # Override the storageClass specified in sharedStorageClassName
    # storageClass: "shared-storage"
    size: 1Gi
    labels: {}
  caCertificates:
    # Extra root certificates are disabled by default
    enabled: false
    # Load custom Certificates from the following ConfigMap
    configMapName: cacert

jobs:
  #
  # Set security contexts for pre- and post-install hooks
  #
  securityContext: {}

  podSecurityContext: {}

  db:
    preInstallMigrator:
      # Additional job labels
      labels: {}
      # Additional job annotations
      annotations: {}
      # Additional pod labels
      podLabels: {}
      # Additional pod annotations
      podAnnotations: {}
    postInstallMigrator:
      # Additional job labels
      labels: {}
      # Additional job annotations
      annotations: {}
      # Additional pod labels
      podLabels: {}
      # Additional pod annotations
      podAnnotations: {}

  setup:
    # Additional job labels
    labels: {}
    # Additional job annotations
    annotations: {}
    # Additional pod labels
    podLabels: {}
    # Additional pod annotations
    podAnnotations: {}
  cleanup:
    # Enabled by default, disable to retain resources after uninstall
    enabled: true
    # Additional job labels
    labels: {}
    # Additional job annotations
    annotations: {}
    # Additional pod labels
    podLabels: {}
    # Additional pod annotations
    podAnnotations: {}
  secret:
    # Additional job labels
    labels: {}
    # Additional job annotations
    annotations: {}
    # Additional pod labels
    podLabels: {}
    # Additional pod annotations
    podAnnotations: {}

#
# Configure service account for pre- and post-install hooks
#
serviceAccount:
  name: service-account
  # Certain instances will require the creation of a pre-deployed service account. For instance, AWS IAM enabled service accounts need to be created outside
  # of the chart to allow for setting of permissions on other AWS services like Secrets Manager
  deployRolesOnly: false

#
# Configure database
#
database:
  # deploy the database pod?  if false, a connection string to a SQL Server will need to be provided through one of the configuration providers.
  enabled: true
  # Labels to add to the MSSQL deployment
  labels: {}
  # Annotations to add to the MSSQL deployment
  annotations: {}
  # Additional labels to the MSSQL pod
  podLabels: {}
  # Additional annotations to the MSSQL pod
  podAnnotations: {}
  # Optionally place deployment on specifc nodes
  nodeSelector: {}
  # Optionally add taint tolerations
  tolerations: []
  # Image name, tag, and pull policy
  image:
    name: registry.lab.io:8443/mssql/server # mirrored-image
    # Tag of the image to use. (Defaults to general.coreVersion)
    tag: 2022-CU12-ubuntu-22.04
  # The container is limited to the resources below.  Adjust for your environment.
  resources:
    requests:
      memory: "2G"
      cpu: "100m"
    limits:
      memory: "2G"
      cpu: "500m"

  # The MSSQL volumes for the PVCs
  volume:
    backups:
      # Use an existing PVC by specifying the name.
      # existingClaim: claimName
      # Override the accessMode specified in general.volumeAccessMode
      # accessMode: ReadWriteOnce
      # Override the storageClass specified in sharedStorageClassName
      # storageClass: "shared-storage"
      size: 1Gi
      labels: {}
      annotations: {}
    data:
      # Use an existing PVC by specifying the name.
      # existingClaim: claimName
      # Override the accessMode specified in general.volumeAccessMode
      # accessMode: ReadWriteOnce
      # Override the storageClass specified in sharedStorageClassName
      # storageClass: "shared-storage"
      size: 10Gi
      labels: {}
      annotations: {}
    log:
      # Use an existing PVC by specifying the name.
      # existingClaim: claimName
      # Override the accessMode specified in general.volumeAccessMode
      # accessMode: ReadWriteOnce
      # Override the storageClass specified in sharedStorageClassName
      # storageClass: "shared-storage"
      size: 10Gi
      labels: {}
      annotations: {}
  # Specifies the strategy used to replace old Pods by new ones. The value can be "OnDelete" or "RollingUpdate". "OnDelete" is the default value.
  # When set to OnDelete, the SQL pod will not be recreated automatically on update.  It must be manually deleted to update.
  updateStrategy: OnDelete
  # This will set the Kubernetes container security context
  securityContext: {}
  # This will set the Kubernetes pod security context (including fsGroup)
  podSecurityContext:
    fsGroup: 10001 # OCP: Needed on OCP for the database to copy data, scc issue
  # Run the pod under a service account you create.  This is especially useful for OpenShift deployments
    podServiceAccount: "bitwarden-sa" # OCP: The service account created with anyuuid

# You can specify raw Kubernetes manifests that will be applied before or after the base Helm install.
# Please see the chart README in GitHub for more information and examples.
rawManifests: # OCP: Set Routes for all services in postInstall, this is fully utiling Routes with the builtin TLS certs
  preInstall: []
  postInstall:
  - kind: Route
    apiVersion: route.openshift.io/v1
    metadata:
      name: bitwarden-self-host-web
      namespace: bitwarden
      annotations:
        haproxy.router.openshift.io/rewrite-target: /
    spec:
      host: bitwarden.apps.ocp.lab.io
      path: "/"
      to:
        kind: Service
        name: bitwarden-self-host-web
        weight: 100
      port:
        targetPort: 5000
      tls:
        termination: edge
        insecureEdgeTerminationPolicy: Redirect
        destinationCACertificate: ''
  - kind: Route
    apiVersion: route.openshift.io/v1
    metadata:
      name: bitwarden-self-host-api
      namespace: bitwarden
      annotations:
        haproxy.router.openshift.io/rewrite-target: /
    spec:
      host: bitwarden.apps.ocp.lab.io
      path: "/api/"
      to:
        kind: Service
        name: bitwarden-self-host-api
        weight: 100
      port:
        targetPort: 5000
      tls:
        termination: edge
        insecureEdgeTerminationPolicy: Redirect
        destinationCACertificate: ''
  - kind: Route
    apiVersion: route.openshift.io/v1
    metadata:
      name: bitwarden-self-host-attachments
      namespace: bitwarden
      annotations:
        haproxy.router.openshift.io/rewrite-target: /
    spec:
      host: bitwarden.apps.ocp.lab.io
      path: "/attachments/"
      to:
        kind: Service
        name: bitwarden-self-host-attachments
        weight: 100
      port:
        targetPort: 5000
      tls:
        termination: edge
        insecureEdgeTerminationPolicy: Redirect
        destinationCACertificate: ''
  - kind: Route
    apiVersion: route.openshift.io/v1
    metadata:
      name: bitwarden-self-host-icons
      namespace: bitwarden
      annotations:
        haproxy.router.openshift.io/rewrite-target: /
    spec:
      host: bitwarden.apps.ocp.lab.io
      path: "/icons/"
      to:
        kind: Service
        name: bitwarden-self-host-icons
        weight: 100
      port:
        targetPort: 5000
      tls:
        termination: edge
        insecureEdgeTerminationPolicy: Redirect
        destinationCACertificate: ''
  - kind: Route
    apiVersion: route.openshift.io/v1
    metadata:
      name: bitwarden-self-host-notifications
      namespace: bitwarden
      annotations:
        haproxy.router.openshift.io/rewrite-target: /
    spec:
      host: bitwarden.apps.ocp.lab.io
      path: "/notifications/"
      to:
        kind: Service
        name: bitwarden-self-host-notifications
        weight: 100
      port:
        targetPort: 5000
      tls:
        termination: edge
        insecureEdgeTerminationPolicy: Redirect
        destinationCACertificate: ''
  - kind: Route
    apiVersion: route.openshift.io/v1
    metadata:
      name: bitwarden-self-host-events
      namespace: bitwarden
      annotations:
        haproxy.router.openshift.io/rewrite-target: /
    spec:
      host: bitwarden.apps.ocp.lab.io
      path: "/events/"
      to:
        kind: Service
        name: bitwarden-self-host-events
        weight: 100
      port:
        targetPort: 5000
      tls:
        termination: edge
        insecureEdgeTerminationPolicy: Redirect
        destinationCACertificate: ''
  - kind: Route
    apiVersion: route.openshift.io/v1
    metadata:
      name: bitwarden-self-host-scim
      namespace: bitwarden
      annotations:
        haproxy.router.openshift.io/rewrite-target: /
    spec:
      host: bitwarden.apps.ocp.lab.io
      path: "/scim/"
      to:
        kind: Service
        name: bitwarden-self-host-scim
        weight: 100
      port:
        targetPort: 5000
      tls:
        termination: edge
        insecureEdgeTerminationPolicy: Redirect
        destinationCACertificate: ''
  - kind: Route
    apiVersion: route.openshift.io/v1
    metadata:
      name: bitwarden-self-host-sso
      namespace: bitwarden
      annotations:
        # Rewrite will not work with sso
    spec:
      host: bitwarden.apps.ocp.lab.io
      path: "/sso/"
      to:
        kind: Service
        name: bitwarden-self-host-sso
        weight: 100
      port:
        targetPort: 5000
      tls:
        termination: edge
        insecureEdgeTerminationPolicy: Redirect
        destinationCACertificate: ''
  - kind: Route
    apiVersion: route.openshift.io/v1
    metadata:
      name: bitwarden-self-host-identity
      namespace: bitwarden
      annotations:
        # Rewrite will not work with identity
    spec:
      host: bitwarden.apps.ocp.lab.io
      path: "/identity/"
      to:
        kind: Service
        name: bitwarden-self-host-identity
        weight: 100
      port:
        targetPort: 5000
      tls:
        termination: edge
        insecureEdgeTerminationPolicy: Redirect
        destinationCACertificate: ''
  - kind: Route
    apiVersion: route.openshift.io/v1
    metadata:
      name: bitwarden-self-host-admin
      namespace: bitwarden
      annotations:
        # Rewrite will not work with admin
    spec:
      host: bitwarden.apps.ocp.lab.io
      path: "/admin"
      to:
        kind: Service
        name: bitwarden-self-host-admin
        weight: 100
      port:
        targetPort: 5000
      tls:
        termination: edge
        insecureEdgeTerminationPolicy: Redirect
        destinationCACertificate: ''
